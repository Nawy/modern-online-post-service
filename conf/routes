
# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~



GET           /users/:id        controllers.UserController.get(id:String)
POST          /users            controllers.UserController.save
DELETE        /users/:id        controllers.UserController.delete(id:String)

GET           /messages         controllers.MessageController.get(email: String, opponent: String)
POST          /messages         controllers.MessageController.send


      